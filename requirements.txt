# Backend
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
python-multipart>=0.0.9

# PDF -> text (постраничный)
pymupdf>=1.24

# Vector store / math
numpy>=1.26

# LLM / embeddings
yandex-cloud-ml-sdk>=0.10   # провайдер по умолчанию (Yandex Cloud)
openai>=1.30                # для LLM_PROVIDER=openai (OpenAI-совместимый эндпоинт)

# Crawler
requests>=2.31
beautifulsoup4>=4.12

# Telegram bot
pyTelegramBotAPI>=4.14

# Eval
pyyaml>=6.0
