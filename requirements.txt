# Backend
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
python-multipart>=0.0.9

# PDF -> text (постраничный)
pymupdf>=1.24

# Vector store / math
numpy>=1.26

# Crawler + OpenAI REST (LLM/эмбеддинги вызываются через requests напрямую —
# официальный openai-SDK ломает URL на Python 3.14, см. app/openai_rest.py)
requests>=2.31
beautifulsoup4>=4.12

# Telegram bot
pyTelegramBotAPI>=4.14

# Eval
pyyaml>=6.0
