# secrets / env
.env
.env.local
/api/config.py
/telegram/config.py

# python
__pycache__/
*.pyc
.venv/
venv/

# regenerable index (rebuild with: python -m scripts.reindex)
/data/index/

# Корпус PDF (data/docs/*.pdf) КОММИТИМ целиком — ~224 МБ, макс. файл 19 МБ,
# в лимиты GitHub укладывается. scripts/fetch_sources.py оставлен как опция
# на случай, если корпус решат вынести из git.

# os
.DS_Store
votes.json
